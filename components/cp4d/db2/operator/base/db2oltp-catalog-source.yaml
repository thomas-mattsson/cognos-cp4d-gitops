apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: ibm-db2oltp-cp4d-operator-catalog
  namespace: openshift-marketplace
spec:
  displayName: IBM Db2oltp CP4D Catalog
  image: icr.io/cpopen/ibm-db2oltp-cp4d-operator-catalog:latest
  publisher: IBM
  sourceType: grpc
  updateStrategy:
    registryPoll:
      interval: 45m
